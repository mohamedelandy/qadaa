# Flow: 03 - Wizard Quick Setup
# Suite: smoke
# Covers: skip onboarding, complete the 4-step wizard, land on dashboard.
# Prerequisites: none — flow 02 provides the cold launch + skip.
# clearState: false (02_onboarding_skip.yaml already does launchApp with clearState: true — do NOT re-launch)
# Note: IntentionSheet dismissal lives in ../utils/setup_wizard.yaml — do not repeat it here.
appId: com.melnady.qadaa
---
- runFlow: ../flows/02_onboarding_skip.yaml
- runFlow: ../utils/setup_wizard.yaml
- assertVisible:
    id: dashboard-screen